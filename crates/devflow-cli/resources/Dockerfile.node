# Devflow default Node.js container execution image
FROM node:22-slim

# Install system dependencies if required by native extensions
RUN apt-get update && apt-get install -y \
    build-essential \
    python3 \
    curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
